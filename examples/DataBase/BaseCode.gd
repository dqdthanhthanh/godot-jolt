"""
Messenger.push_notification(0,data.teams[i].name + " team doesn't have enough 7 people in the squad")
Messenger.btnYes.disconnect("pressed",Callable(self,"create_match_data"))
Messenger.btnYes.connect("pressed",Callable(self,"create_match_data").bind(0))
get_tree().get_root().get_node("Season").get_node("%MatchInfo")
		var turn = insLabel.instantiate()
		cont.add_child(turn)
await get_tree().create_timer(value).timeout
https://github.com/Federico-Ciuffardi/GodotTouchInputManager
tween.connect("finished",Callable(self,"player_set_hold_ball"))
print(OS.get_data_dir()) lay data goc
"""
## Round "%.2f" % float(10)
## BreakLine "\n" + "Hello"
## Waittime quick await get_tree().create_timer(0.4).timeout
"""
Can tim goc Arrow ???
Tim vi tri can dua bong: ballRange AIPowerShot
Tim luc bong phu hop: AIPowerShot = (unit.statShotPower+unit.statSpeed)/2
Tim khoang cach khong gian:
	DK target o giua: chon random (-x,+x)
	DK target o tren hoac duoi: chon random o giua
Tg = -1.5: BallPos 0-1.5
Tg =  1.5: BallPos 0-(-1.5)
Tg = 0   : BallPos (-1.5,0) (0,1.5)
	DK target gan goal power <
	Dk target xa goal power >
"""

